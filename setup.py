from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='The project is about identifying the bad wafer from list of all availables wafers.',
    author='TCS',
    license='MIT',
)
